# permetic-push

`ac.onyx.permetic:permetic-push` — FCM push capability for Permetic. Optional artifact;
omit it from the build and `available('push')` returns `false`.

Read `../../specs/01-permetic-webview.md` (task 7) and `../../CLAUDE.md` before changing
anything here.
