# permetic-ota

npm `permetic-ota` — CLI that builds, signs, and publishes OTA bundles of the web app
for Permetic's on-device resolver (spec 01, "Asset serving, offline, and OTA").

Concept-level; no implementation yet. Read `../../specs/06-permetic-ota.md` before
starting work here.
