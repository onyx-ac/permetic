<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <activity
            android:name="ac.onyx.permetic.transport.android.TestActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            The three attributes spec 07 requires of a PiP host. configChanges is the
            load-bearing one: without it the Activity is recreated on the PiP
            transition, the WebView is torn down, and the page reloads - which for a
            canvas-streamed tracker destroys the session being watched (ADR 0011).
        -->
        <activity
            android:name="ac.onyx.permetic.pip.PipProbeActivity"
            android:exported="true"
            android:supportsPictureInPicture="true"
            android:resizeableActivity="true"
            android:configChanges="screenSize|smallestScreenSize|screenLayout|orientation" />
    </application>
</manifest>
