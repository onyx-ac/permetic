# permetic-billing

`ac.onyx.permetic:permetic-billing` — Play Billing capability for Permetic. Optional
artifact; omit it from the build and `available('billing')` returns `false`.

Read `../../specs/01-permetic-webview.md` (task 8) and `../../CLAUDE.md` before changing
anything here.
